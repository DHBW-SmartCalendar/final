package com.example.notificationplanner.ui.form

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

@Composable
fun CalendarForm (){
    Text(text ="CalendarForm" )
    // TODO Heading
    // TODO implement slider for event amount ->(use States / on change : updating of data object is required) use WeatherForm as reference
}